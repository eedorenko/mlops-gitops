apiVersion: scheduler.kalypso.io/v1alpha1
kind: Workload
metadata:
  name: entree-detection
  labels:
    application: kos
    type: model
    task: entree-detection
spec:
  deploymentTargets:
    - name: dev
      labels:
        purpose: dev-test
      environment: dev
      manifests:
        repo: https://github.com/eedorenko/mlops-gitops-gitops
        branch: dev
        path: ./entree-detection
    - name: stage
      labels:
        purpose: functional-test
      environment: stage
      manifests:
        repo: https://github.com/eedorenko/mlops-gitops-gitops
        branch: stage
        path: ./entree-detection
    - name: prod-gray
      labels:
        purpose: prod
      environment: prod
      manifests:
        repo: https://github.com/eedorenko/mlops-gitops-gitops
        branch: prod-gray
        path: ./entree-detection
